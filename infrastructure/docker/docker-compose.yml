version: '3.8'

services:
  # Database
  postgresql:
    image: postgres:15
    container_name: altera-postgresql
    environment:
      POSTGRES_DB: altera_school_db
      POSTGRES_USER: altera_user
      POSTGRES_PASSWORD: altera_password
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgresql_data:/var/lib/postgresql/data
      - ./init-scripts:/docker-entrypoint-initdb.d
    networks:
      - altera-network

  # Service Discovery
  eureka-server:
    image: openjdk:17-jdk-slim
    container_name: altera-eureka
    ports:
      - "8761:8761"
    networks:
      - altera-network

  # API Gateway
  api-gateway:
    image: openjdk:17-jdk-slim
    container_name: altera-gateway
    ports:
      - "8080:8080"
    depends_on:
      - eureka-server
    networks:
      - altera-network

  # Microservices
  auth-service:
    build:
      context: ../../services/school-springboot/auth-service
      dockerfile: Dockerfile
    container_name: altera-auth-service
    ports:
      - "8081:8081"
    depends_on:
      - postgresql
      - eureka-server
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgresql:5432/altera_school_db
      - SPRING_DATASOURCE_USERNAME=altera_user
      - SPRING_DATASOURCE_PASSWORD=altera_password
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka
    networks:
      - altera-network

  student-service:
    build:
      context: ../../services/school-springboot/student-service
      dockerfile: Dockerfile
    container_name: altera-student-service
    ports:
      - "8082:8082"
    depends_on:
      - postgresql
      - eureka-server
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgresql:5432/altera_school_db
      - SPRING_DATASOURCE_USERNAME=altera_user
      - SPRING_DATASOURCE_PASSWORD=altera_password
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka
    networks:
      - altera-network

  teacher-service:
    build:
      context: ../../services/school-springboot/teacher-service
      dockerfile: Dockerfile
    container_name: altera-teacher-service
    ports:
      - "8083:8083"
    depends_on:
      - postgresql
      - eureka-server
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgresql:5432/altera_school_db
      - SPRING_DATASOURCE_USERNAME=altera_user
      - SPRING_DATASOURCE_PASSWORD=altera_password
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka
    networks:
      - altera-network

  course-service:
    build:
      context: ../../services/school-springboot/course-service
      dockerfile: Dockerfile
    container_name: altera-course-service
    ports:
      - "8084:8084"
    depends_on:
      - postgresql
      - eureka-server
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgresql:5432/altera_school_db
      - SPRING_DATASOURCE_USERNAME=altera_user
      - SPRING_DATASOURCE_PASSWORD=altera_password
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka
    networks:
      - altera-network

  attendance-service:
    build:
      context: ../../services/school-springboot/attendance-service
      dockerfile: Dockerfile
    container_name: altera-attendance-service
    ports:
      - "8085:8085"
    depends_on:
      - postgresql
      - eureka-server
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgresql:5432/altera_school_db
      - SPRING_DATASOURCE_USERNAME=altera_user
      - SPRING_DATASOURCE_PASSWORD=altera_password
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka
    networks:
      - altera-network

  grade-service:
    build:
      context: ../../services/school-springboot/grade-service
      dockerfile: Dockerfile
    container_name: altera-grade-service
    ports:
      - "8086:8086"
    depends_on:
      - postgresql
      - eureka-server
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgresql:5432/altera_school_db
      - SPRING_DATASOURCE_USERNAME=altera_user
      - SPRING_DATASOURCE_PASSWORD=altera_password
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka
    networks:
      - altera-network

  # Frontend Applications
  teacher-portal:
    build:
      context: ../../apps/teacher-portal
      dockerfile: Dockerfile
    container_name: altera-teacher-portal
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://api-gateway:8080
    networks:
      - altera-network

  admin-portal:
    build:
      context: ../../apps/admin-portal
      dockerfile: Dockerfile
    container_name: altera-admin-portal
    ports:
      - "3001:3001"
    environment:
      - VITE_API_BASE_URL=http://api-gateway:8080
    networks:
      - altera-network

volumes:
  postgresql_data:

networks:
  altera-network:
    driver: bridge
